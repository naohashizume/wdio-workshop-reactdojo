// #2b: import the page object class instance

describe('Inventory page', () => {
  beforeEach(() => {
    //  #2c: add navigation to 'https://www.saucedemo.com/inventory.html'
  });

  afterEach('reset app state', function() {
    //  #2d: add method to reset shopping cart after each test
  });

  it('add one item to cart', () => {
    // #2e: open url, and add an item in shopping cart
    // #2f: add an assertion
  });

  it('removes one item', () => {
    // #2g: open url, and add an item in shopping cart, then remove the item
    // #2h: add an assertion
  });
});
